== README

This is a little project to show some things like:

* Image treatment with Carrierwave
* Basic Api consuming with Httparty (From Spotify)
* Save objects, nested forms, and classic Rails stuff

To run it:

* Clone it

* `cd path_to_project`

* `bundle install`

* `rake db:migrate`

* `rails s`
